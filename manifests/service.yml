apiVersion: v1
kind: Service
metadata:
    name: amarroyazuredevopsgithubacraksapp
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: amarroyazuredevopsgithubacraksapp